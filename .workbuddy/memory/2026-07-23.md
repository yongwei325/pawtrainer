# 2026-07-23 工作日志

## 项目：英文狗狗训练/宠物行为静态站（流量+广告变现）
- 方向已与用户敲定：零预算、Solo、AI 辅助建站+内容；静态站（Hugo + Cloudflare Pages）；面向美/英/加/澳英语用户；AdSense + 联盟双收入。
- niche 可行性已用 WebSearch 实测验证：需求巨大（dog training tips 9万/月等）、头部被 AKC/PetMD 占但长尾有缝隙、RPM 高（Mediavine $25–50）、联盟佣金高（Brain Training 75%、$31/单；K9 TI $103–148/单）。
- 计划书：`C:\Users\Yongwei\.workbuddy\plans\swift-nebula-babbage.md`
- 用户评审后要求加两项优化（已写入计划）：
  1. 差异化定位 USP = 正向强化/科学派（force-free, science-based），不写惩罚式、不写医疗。
  2. 长尾实测验证步骤（P-1：SERP 抽查前10，只写高/中级机会词）。
  3. 站外引流渠道以 Pinterest 为主 + Reddit/Quora/HARO 轻量补充。
- 当前状态：计划已完整，待用户批准进入 Craft 模式生成文件（Hugo 骨架、已验证关键词地图、3 文章模板、示范文、README/Pinterest SOP）。
- 待用户自办：域名购买、Cloudflare/AdSense/联盟/Pinterest 账号注册。

## UI 设计交付（UI Designer 角色）
- 用户要求基于计划做前端页面 UI 设计。已用 impeccable 技能方法论产出完整设计稿。
- 品牌名定为占位 **"Paw & Praise"**，定位文案 Force-free, science-based。
- 设计语言：有机编辑风、暖纸米色背景 + 鼠尾草绿(品牌) + 暖琥珀(CTA)，OKLCH 色彩；标题 Fraunces + 正文 Newsreader + UI 系统无衬线（仅2个 webfont 保速度）；爪印母题 + 手绘下划线 + 正向强化徽章。
- 交付文件：`D:/Yongwei/探讨/ui-design/styles.css`（设计令牌+组件）、`index.html`（首页）、`article.html`（文章模板含 AdSense 广告位/联盟卡/作者卡/免责）、`design-system.md`（交付规范）。
- 已内置：明/暗主题切换、GDPR Cookie 条、响应式(900/680 断点)、WCAG AA 对比度与焦点环、reduced-motion。
- 后续：移植到 Hugo 模板、替换占位图为真实照片(2:3 Pinterest pin)、注入 AdSense/联盟、加 sitemap/JSON-LD。
- 用户决策：**全站不需要邮件订阅/Newsletter 功能**。已清理 index.html 的订阅板块、styles.css 的 `.newsletter`/`.field` 样式、design-system.md 的 3 处引用。移植到 Hugo 时也不要加订阅模块（注：用户此前明确站外引流以 Pinterest 为主，不使用邮件列表）。

## 后台/后端实现（Senior Developer 角色）
- 用户要"后台简单编辑发布文章"。因零预算+静态站前提，选定 **Decap CMS**（git 驱动、免费、无服务器无数据库），而非 Laravel 自建后端（需付费 PHP 主机）。用户表示"不懂差别，选最简单的"。
- 已搭最小可运行 Hugo 项目（站点根目录）：config.toml + archetypes + layouts(_default/baseof,single,list + index) 占位模板 + content/posts 一篇示范文 + static/admin(Decap index.html + config.yml，含 posts 集合与 pages 集合 About/Privacy/Disclaimer)。
- 用下载的 hugo v0.128.0 真实构建验证通过：文章页生成、2 个 AdSense 广告位、联盟卡(rel=sponsored nofollow)、作者 E-E-A-T 块均正确渲染，并出现在首页/列表；draft=true 文章被正确隐藏。
- **关键坑（已记文档 ADMIN.md）**：Hugo 默认不发布"发布时间晚于当前"的文章——Decap 日期若选未来会被静默隐藏。样例中招，已将日期改过去时间解决。
- 交付 ADMIN.md：GitHub OAuth 应用创建(client_id 填 config.yml)、Cloudflare Pages(hugo --minify / public)、/admin 登录发布流程、draft 开关、未来日期坑。
- 加 .gitignore 忽略 /public /resources。占位模板后续替换为 ui-design/ 的 premium 设计；广告/联盟代码届时注入 `.ad`/`.affiliate`。
- 待用户自办：建 GitHub 仓库并推送、建 OAuth 应用、连 Cloudflare Pages、替换 config 里的 repo/branch/baseURL/client_id 占位。
